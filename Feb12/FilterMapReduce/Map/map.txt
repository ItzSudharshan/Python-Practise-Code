The map() function takes two arguments: a function and an iterable (like a list). It returns a map object, which applies the given function to every item in the iterable. You need to convert it to a list to see the results.

he map() function applies a given function to each item in an iterable (like a list) and returns a map object.