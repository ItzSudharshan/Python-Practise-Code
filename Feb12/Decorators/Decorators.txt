A decorator in Python is a function that takes another function as input and adds extra functionality to it without modifying its core. It wraps the original function in an additional layer of logic, enhancing its behavior. 

The decorator is applied to a function using the @ symbol.
